# Pub_Sub_Framework · 开发记录

> 最近整理：2026-09-14 · 当前提交：`8144ef4`（`temp`）
> 对比基线：`8144ef4`
> 工作区：存在未提交代码变化

这是一套用 C++17 编写的 Broker 风格发布订阅框架。最初的使用场景是机器人或
嵌入式控制中的数据分发，示例把 IMU 数据放进 `Pitch` 频道，再交给 PID 计算器。

## 提交时间线

### 01 · `e84bcb3` · 2026-08-03

仓库从规则文件起步：加入 `.gitattributes` 和 `.gitignore`，先约定换行符、
编码处理和常见构建产物的忽略范围。

这次提交还没有业务代码，但它把“哪些东西该进仓库、哪些东西只留在本机”先分清楚了。
对一个后续要频繁生成构建目录和 IDE 文件的 C++ 项目来说，这是比较稳的第一步。

### 02 · `edd01a4` · 2026-08-03

用 Visual Studio 模板创建最初工程，加入 `.slnx`、`.vcxproj`、
`.vcxproj.filters` 和 `Pub_Sub_Framework.cpp`。

此时项目还是标准控制台程序形态，入口文件里只有模板代码。发布订阅框架的概念还没落地，
但工程已经可以被 IDE 打开、构建和调试。

### 03 · `718b1d6` · 2026-08-03

入口文件从 `Pub_Sub_Framework.cpp` 改名为 `main.cpp`。

这是一个很小的提交，但方向很明确：文件名开始脱离 Visual Studio 项目模板，
转向更通用的 C/C++ 工程习惯。这个动作也给后面拆分 `Broker`、
`Pub_Sub` 等模块留出了空间。

### 04 · `df43661` · 2026-08-03

引入 `Broker.hpp`，并把原来的实现文件改成 `Broker.cpp`。

这是架构意图第一次露出来：项目不只是一个普通入口程序，而是准备围绕 Broker
做消息中转。虽然这个阶段还没有真正的发布、订阅或投递实现，但“中介层”已经成为代码命名里的核心对象。

### 05 · `bf7054f` · 2026-08-03

删除 Visual Studio 解决方案和工程文件，把代码移动到 `Inc/` 与 `Src/` 目录。

这次提交完成了第一轮结构调整：项目不再强绑定 MSVC 工程文件，而是开始采用头文件和源文件分离的普通源码布局。
当时 `Broker.cpp`、`Broker.hpp` 的位置仍有些试探性，但目录边界已经立起来了。

### 06 · `ee4f4ba` · 2026-08-03

加入 `CMakeLists.txt`，并建立 `Broker`、`Msg`、`Pub_Sub`、`Topic` 四个模块骨架。

这是项目从“有文件”走向“有工程”的关键提交：CMake 指定 C++17 和可执行文件目标，
源码也按职责拆成消息中转、消息体、发布订阅关系和主题概念四块。虽然每个模块还只是空壳，
但后续功能已经有了可以落脚的位置。

### 07 · `ce015f3` · 2026-08-04

补齐 `Publisher`、`Subscriber` 的类骨架，恢复 `main` 入口，并加入 VS Code 构建任务。

`Src/main.cpp` 里出现了 `IMU` 和 `PID_Calculer`，说明最初的使用场景更偏机器人或嵌入式控制：
IMU 发布姿态数据，PID 计算器订阅相关频道。此时类和示例对象已经有了，但注册函数还没有实质行为。

### 08 · `d961a19` · 2026-08-04

补上 Debug 构建与 VS Code 调试配置，`CMakeLists.txt` 默认使用 Debug，
`launch.json` 和 `tasks.json` 让本地断点调试路径基本跑通。

`Publisher::Register` 与 `Subscriber::Register` 在这次提交中有了函数体，
但内容仍然只是 TODO。换句话说，工程已经能启动和调试，类也能被调用，
但发布订阅链路还停在“接口占位”的阶段。

### 09 · `934e455` · 2026-09-06

实现频道注册表 `Reg_Map`，并加入两个 `Map_Add` 重载：

- 发布者注册时可以先创建频道占位。
- 订阅者注册时可以加入对应频道，并避免重复登记同一个订阅者指针。

这是第一个真正形成运行语义的提交：`main.cpp` 中 `IMU` 与 `PID_Calculer`
都注册到 `"Pitch"` 频道，代码开始具备“频道 -> 订阅者列表”的关系。

同一轮里还删除了暂时没有内容的 `Topic` 模块，把频道职责先收进 `Pub_Sub`；
并把本地工具链从 VS2022 方向切到 llvm-mingw、clangd 和 lldb-mi。需要注意的是，
`Map_mtx` 虽然已经声明，但注册表写入还没有真正加锁。

### 10 · `8144ef4` · 2026-09-13

开始从“只有注册表”推进到“可以描述一条消息”的阶段。

`Msg` 增加 `counter` 和 `std::any payload`，表示消息序号和泛型载荷；
`Publisher` 增加 `Notify` 模板函数，创建 `std::shared_ptr<Msg>` 后按当前频道遍历
`Reg_Map`，再把消息推给每个订阅者。发布者和订阅者也都加入了互斥量、
条件变量等成员，说明后续准备做等待和通知机制。

这一版仍是半成品：`Subscriber::Msg_Push` 只有声明没有实现，`Notify` 没有处理频道查找失败，
也还没有推进 `Num_Cnt`。它的价值在于把下一步的接口形状摆出来了：
消息体、发布入口、订阅者收包入口都已经出现，后面要做的是让这条路径真正闭环并可测试。

## 未提交变化

### 工作区 · 2026-09-14

`Src/Pub_Sub.cpp` 正在继续补订阅者收包方向：新增了 `Subscriber::Msg_Push`
的函数体，并在内部先用 `std::lock_guard<std::mutex>` 锁住订阅者互斥量。
这说明收包路径已经从“只有头文件声明”推进到源文件实现位置。

不过这批变化还不能当成完成项：`Subscriber::Register` 里新增的
`std::thread([this]())` 目前是不完整语句，会让源码无法通过编译；
`Msg_Push` 也只是空实现，还没有保存消息、唤醒等待线程或触发消费逻辑。
这部分应继续作为未提交的半成品处理。

## 当前结构

- `Inc/Pub_Sub.hpp`：`Publisher`、`Subscriber` 和频道注册表声明。
- `Src/Pub_Sub.cpp`：频道创建与订阅者登记的实现。
- `Inc/Msg.hpp`：消息载荷的试验性定义，使用 `std::any` 保存泛型数据。
- `Broker.hpp` / `Broker.cpp`：保留的中转层位置，尚未实现。
- `Inc/all.hpp`：公共头文件和全局 `Map_mtx`。
- `Topic` 模块：已删除，频道职责暂时放在 `Pub_Sub` 中。

## 当前已知问题

1. 未提交的 `Subscriber::Register` 中存在不完整的 `std::thread([this]())` 语句，
   当前源码会在编译阶段失败。
2. `Subscriber::Msg_Push` 虽然在未提交代码中补了函数体，但仍只是加锁空实现，
   还没有保存消息、通知消费者或处理载荷。
3. `Publisher::Notify` 还没有处理未注册频道或查找失败的情况，也没有推进 `Num_Cnt`。
4. `Map_mtx` 已声明但没有用于保护 `Reg_Map`，并发注册和投递仍不安全。
5. `Reg_Map` 保存裸 `Subscriber*`，析构函数尚未注销订阅者，存在悬垂指针风险。
6. 注册表是头文件中的全局对象，封装边界还比较粗糙。
7. 还没有可重复的构建/测试验证；当前示例源码只完成注册后打印文本。

本次整理只更新记忆和开发记录文件，未修改 `Inc/`、`Src/` 或构建配置。
## 未提交变化补充

2026-09-14 新增的未提交代码涉及 `Inc/Pub_Sub.hpp` 和 `Src/Pub_Sub.cpp`：

- `Subscriber::Register` 启动线程，通过 `condition_variable` 等待消息；
- `Subscriber::Msg_Push` 在互斥锁保护下暂存消息并唤醒等待线程；
- `Subscriber::Get_Value<T>` 提供读取已接收数据的模板接口；
- `temp_data` 与 `data` 分离，区分待处理消息和可读取数据。

这仍是未提交的工作区变化，尚未完成端到端构建和运行验证。线程使用 `detach`，析构时存在对象生命周期风险；当前也只保留一条待处理消息，连续推送时不会排队。
