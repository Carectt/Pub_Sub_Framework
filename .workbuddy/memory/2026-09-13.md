# 2026-09-13 · 记忆整理

- 重新整理长期记忆、开发记录和自动化记录，去掉重复的“无变化”巡检流水账。
- 当前提交是 `8590d0f`；消息体和等待机制刚开始搭建，仍有编译与接口问题。
- 本次只修改 `.workbuddy/` 记忆文件和 `DevLog.md`，没有改动 `Inc/`、`Src/` 或
  构建配置。

## VSCode"创建定义/声明"功能调研（15:09–15:20，补记）

- 右键"创建定义/声明"是 cpptools IntelliSense 的官方功能；切 clangd 后
  `C_Cpp.intelliSenseEngine` 被设为 disabled 导致功能消失。clangd 原生无
  Implement Method，替代是 DefineOutline（Ctrl+. → Move function body to source file）。
- C-mantic（ID `tdennis4496.cmantic`）官方支持 clangd，但停更于 2021-06（v0.9.0），
  已知 bug：#72 忽略命名空间（VS Code 1.102.1 已修其中 VS Code 回归部分）、
  #71 clangd 下 Update Signature 不弹出、#67/#64 rg 进程 CPU 100%。
- Fork 链 BigBahss → sr-tream → ReDDarKwh（v0.9.4，2025 年初零星维护）均未上市场。
- 结论：无活跃维护的第三方同类扩展。用户选方案 A：双引擎并存。
- 15:22 已改 `.vscode/settings.json`：`C_Cpp.intelliSenseEngine` 改回 `default`，
  加 `errorSquiggles`/`formatting` = disabled、`intelliSenseUpdateDelay: 2000`。
- 18:20 该文件被误覆写回旧版，已从会话记录恢复方案 A 完整配置（无需 reload 之外的
  额外操作）。
- 18:26 修复 format-on-save 失效：全局 `[cpp]` 默认格式化器是 cpptools，而方案 A
  禁用了 `C_Cpp.formatting`，导致保存格式化静默失效。工作区覆盖
  `[c]`/`[cpp]` 的 `editor.defaultFormatter` 为 `llvm-vs-code-extensions.vscode-clangd`，
  格式化统一归 clangd。用户全局设置：formatOnSave=true、clangd.detectExtensionConflicts=false。
